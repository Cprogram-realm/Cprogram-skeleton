#include <stdio.h>

int main() {
  
  // When performing a textual operation with printf, it is mandatory to put a semicolon at the end of the parameters. It means to finish the process.
  printf("hello, world");
    
  return 0;
}
